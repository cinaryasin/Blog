// docker-compose down / docker-compose up   docker çalıştırma komutu
// powerShell wsl --shutdown  /wsl durdurma işlemi
// docker-compose up -d --build
dotnet restore //paketleri yükleme
dotnet run //projeyi çalıştırma